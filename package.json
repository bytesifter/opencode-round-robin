{
  "name": "opencode-round-robin",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "opencode 插件:多服务商多账号 API key 随机轮询、用量统计、请求日志",
  "type": "module",
  "main": "./dist/index.js",
  "files": ["dist"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.3.9",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
